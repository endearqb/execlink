日期：2026-02-17

内容：
  - 新增功能：
    - 从 0 初始化 ExecLink 的 Tauri + React + TypeScript MVP 工程结构。
    - 新增 Rust 后端命令：初始化状态、CLI 检测、Nilesoft 安装与注册、提权重试、配置写入、立即生效。
    - 新增 Nilesoft 配置生成逻辑：`ai-clis.nss` 覆盖写入与 `shell.nss` import 幂等维护。
    - 新增前端设置页：主开关、NoExit、四个 CLI 开关、安装状态、检测状态、应用配置与立即生效操作。
    - 新增首启自动安装检查流程：初次加载自动尝试安装并在需要时请求提权重试。
    - 新增配置幂等单元测试：验证 `shell.nss` 的 import 不重复写入。
  - 改进：
    - 安装流程支持普通权限失败后的“先提示再提权”重试路径，符合 PRD 的权限策略。
    - 生效流程优先 `shell.exe -restart`，失败自动回退 Explorer 重启。
    - 补充 `README.md` 说明项目运行方式、资源路径与日志目录。
  - 修复：
    - 无（本次为首版 MVP 实现）。

备注：
  - 当前环境缺少 Rust 工具链（`rustc/cargo`），且 npm registry 访问受限，未能完成本地构建与自动化测试。
