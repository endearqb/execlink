日期：2026-02-19

内容：
  - 新增功能：
    - 新增后端命令：`attempt_unregister_nilesoft`、`cleanup_app_data`、`get_cli_install_hints`。
    - Runtime 页新增“恢复与清理”区，支持尝试反注册与二次确认清理应用目录。
    - CLI 检测表新增“安装指引”双入口：复制安装命令、打开官方说明。
    - 诊断信息新增 `app_version`、`build_channel`、`resource_zip_path`。
  - 改进：
    - `apply_config` 配置准备逻辑抽出为可测试函数，明确保留后端 runtime 状态。
    - 补充配置迁移测试（v1/v2/v3 -> 最新版本）和 `shell.exe -restart` 判定测试。
    - 新增发布清单文档：`docs/release_checklist.md`。
    - 修正 `tauri.conf.json` 的 `bundle.icon` 配置，打包烟测可产出 MSI/NSIS 安装包。
    - 新增 CLI 安装方式文档：`docs/cli_install_methods_2026-02-19.md`（含官方命令、前置依赖、来源链接）。
    - 新增安装协助讨论记录：`docs/cli_install_assistance_discussion_2026-02-19.md`（含半自动安装方案与安全边界）。
  - 修复：
    - 增加清理操作的确认令牌机制，避免误删本地运行数据目录。
