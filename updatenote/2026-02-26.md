日期：2026-02-26

内容：
  - 新增功能：
    - 在主界面窗口控制区新增 `?` 按钮（位于最小化按钮前），点击可打开“使用说明向导”弹窗。
    - 新增“使用说明向导”弹窗，提供首次配置的 5 步快速上手流程，并支持遮罩点击与 `Esc` 关闭。
  - 改进：
    - 更新 `README.md`，新增“使用向导（快速上手）”章节，文案与应用内向导保持一致。
    - 版本号升级到 `0.2.4`（`package.json` / `src-tauri/Cargo.toml` / `src-tauri/tauri.conf.json`）。
  - 修复：
    - 统一应用内页脚显示版本到 `0.2.4`，避免与安装包版本不一致。
