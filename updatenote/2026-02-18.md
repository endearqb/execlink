日期：2026-02-18

内容：
  - 改进：
    - 前端 Tauri API 调用增加运行时检测：在非 Tauri 环境下不再抛出 `invoke` 原始 TypeError。
    - 初始化失败提示优化：新增 `tauri_runtime_unavailable` 结果码并给出“请使用 npm run tauri dev”指引。
    - 补齐 Tauri Windows 资源图标：新增 `src-tauri/icons/icon.ico`，修复 `icons/icon.ico not found` 构建失败。
    - 适配 Tauri 2 菜单 API：修复 `src-tauri/src/tray.rs` 中 `MenuBuilder::text` 链式调用错误使用 `?` 导致的编译失败。
    - 修复 Nilesoft 资源定位：`src-tauri/src/nilesoft_install.rs` 改为多候选路径探测（Resource、resource_dir、exe 目录、cwd、`CARGO_MANIFEST_DIR`），兼容 `tauri dev` 与打包运行环境。
    - 资源缺失报错增强：安装失败时会附带“已尝试路径”列表，便于快速定位路径问题。
    - 修复 runtime 状态被前端覆盖：`src-tauri/src/commands.rs` 在 `apply_config` 中保留后端 runtime 字段，避免 `last_activate_at` 被旧配置清空。
    - 修复“立即生效”卡死：`src-tauri/src/explorer.rs` 中 Explorer 兜底重启由阻塞式 `output()` 改为非阻塞 `spawn()`，避免前端长时间 loading。
    - 修正 Nilesoft `-restart` 误判失败：`src-tauri/src/explorer.rs` 对 `exit code=1 且 stderr 为空` 视为可接受结果，避免每次都触发 Explorer 兜底重启。
    - 修复 AI 菜单模板兼容性：`src-tauri/src/nilesoft.rs` 将 `ai-clis.nss` 由 `shell{dynamic{...}}` 改为顶层 `menu(...)` 结构，并将 `type='dir|back.directory'` 调整为 `type='dir|back.dir'`。
    - 调整命令启动目录策略：菜单项改为通过 PowerShell `Set-Location -LiteralPath "@sel.path"` 后再执行 CLI，避免依赖 `dir=@sel.path` 的字段兼容差异。
    - 新增 `Kimi Web` 菜单项支持：右键选择后自动打开终端并执行 `kimi web`。
    - 前后端配置项扩展：新增 `toggles.kimi_web`，UI 增加 `Kimi Web` 开关并将菜单项统计更新为 5 项。
    - 配置版本升级到 `v3`，旧配置自动迁移并默认启用 `kimi_web`。
    - 新增“仅显示 AI 菜单”策略：配置新增 `show_nilesoft_default_menus`，默认关闭（不显示 Nilesoft 默认菜单），可在 UI 开关恢复显示。
    - 新增菜单自定义名称：配置新增 `menu_title`，支持用户将 `AI CLIs` 改为任意名称（如“我的助手们”）。
    - 首页交互重构为 Tab：合并“CLI 检测 + 启用开关”为默认 Tab，并拆分“菜单设置 / 安装生效 / 诊断”分组。
    - 配置版本升级到 `v4`，旧版本自动迁移并补齐 `menu_title/show_nilesoft_default_menus` 默认值。
    - 安装流程增强：`ensure_nilesoft_installed` 完成后自动按当前配置写入 `shell.nss/ai-clis.nss`，首次安装即可应用“仅 AI 菜单”策略。
    - 前端交互重构：CLI 卡片将“检测状态 + 启用状态”合并为三列表格，并将启用控件替换为 `@base-ui/react` 的 `Switch`。
    - 前端交互重构：`Tabs` 切换改为 `@base-ui/react` 的 `Tabs` 组件，默认 Tab 保持在 `CLI`。
    - 布局调整：将“应用配置/立即生效”移动到页面 Header 右侧，CLI 卡片“刷新检测”按钮移动到卡片头右侧同一行。
    - UI 主题统一：将页面颜色重构为 Base UI 官方示例风格的中性灰 + 蓝色强调配色，并以 CSS 变量统一管理按钮、Tabs、Switch、卡片与状态色。
    - 根据示例主题微调控件视觉：Switch 改为灰阶渐变轨道与蓝色焦点环风格；Tabs 改为 Base UI 文档示例的极简分段样式；按钮主题同步为与 Switch 一致的灰阶渐变配色。
    - 进一步做像素级对齐：按你提供的示例参数精调 Tabs/Switch/Button 的尺寸、圆角、阴影、过渡时长与焦点环偏移。
    - 视觉调整：移除 Switch 与按钮的渐变背景，改为纯色主题（保留交互状态与焦点样式）。
    - 视觉调整：移除界面阴影效果（卡片、按钮、Switch 轨道与 Thumb），统一为无阴影平面样式。
    - 交互组件统一：`ToggleRow` 升级为 Base UI `Switch`，菜单与行为设置中的开关与 CLI 开关视觉一致。
    - 视觉微调：去掉按钮与 Switch 的灰色边框，并移除 Tabs 下方分割线。
    - 窗口尺寸调整：保持宽度 `980`，将高度由 `860` 调整为 `645`（3/4）。
    - 视觉微调：Tabs 容器卡片圆角调整为 `12px`，与内部内容卡片圆角保持一致。
  - 验证：
    - 执行 `npx tsc --noEmit`，类型检查通过。
    - 执行 `cargo check --manifest-path src-tauri/Cargo.toml`，Rust 编译检查通过。
    - 执行 `cargo test --manifest-path src-tauri/Cargo.toml nilesoft::tests::should_respect_no_exit_flag`，通过。
    - 执行 `cargo test --manifest-path src-tauri/Cargo.toml nilesoft::tests::should_render_enabled_items_only`，通过。
    - 执行 `npx tsc --noEmit`，前端类型检查通过。
    - 执行 `cargo check --manifest-path src-tauri/Cargo.toml`，后端编译检查通过。
    - 执行 `cargo test --manifest-path src-tauri/Cargo.toml nilesoft::tests::should_render_kimi_web_command`，通过。
    - 执行 `cargo test --manifest-path src-tauri/Cargo.toml nilesoft::tests::should_render_custom_menu_title`，通过。
    - 执行 `cargo test --manifest-path src-tauri/Cargo.toml nilesoft::tests::should_switch_shell_profile_by_default_menu_toggle`，通过。
    - 执行 `npx tsc --noEmit`，前端类型检查通过（含 Base UI Tabs/Switch 组件接入）。
    - 执行 `cargo check --manifest-path src-tauri/Cargo.toml`，后端编译检查通过。
    - 执行 `npx tsc --noEmit`，前端类型检查通过（配色重构后）。
    - 执行 `npx tsc --noEmit`，前端类型检查通过（边框与 Tabs 分割线调整后）。
