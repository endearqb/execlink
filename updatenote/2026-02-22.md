日期：2026-02-22

内容：
  - 新增功能：
    - 完成前端样式体系迁移为 Tailwind CSS（以工具类为主），覆盖 `Home`、`CliConfigTable`、`ToggleRow`、`CliStatusList`。
    - 接入 Tailwind v4 Vite 插件（`@tailwindcss/vite`），构建链正式启用 Tailwind。
  - 改进：
    - 将 `src/styles.css` 重构为最小入口样式，仅保留 Tailwind 导入与基础层（字体、body、code/pre）。
    - 统一按钮、输入框、Switch、Toast、Tabs 的 Tailwind 样式表达，移除旧语义类依赖。
    - 按 `docs/newUI.md` 完成全量新拟态 UI 重制（拿铁色系、三层阴影、圆角体系、按压态反馈），覆盖 CLI/Menu/Runtime/About 全部 Tab。
    - 重构首页骨架与组件视觉层，新增设备感容器、内凹底盘与微发光点缀，同时保留品牌绿作为点缀色。
  - 修复：
    - 清理旧 `styles.css` 大量历史选择器，避免样式混用导致的维护与回归风险。
    - 执行 `npm run build` 验证迁移后 TypeScript 与生产构建通过。
    - 更新 `docs/newUI.md` 增加实现映射章节，确保规范 Token 与实际代码文件一一对应。
